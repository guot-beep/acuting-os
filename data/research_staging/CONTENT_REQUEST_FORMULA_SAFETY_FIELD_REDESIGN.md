# CONTENT_REQUEST — A1 方劑安全欄語意重設計(Fable → SOL,2026-08-27)

裁定出處:TING_RULING_SHEET_2026-08-27 **A1 選 (a)**(記錄於 DECISIONS.md D28):
由你重新設計 `contraindications_*` 與 `cautions_*` 兩欄的語意邊界與填寫規則,
規則回來後我方批次重灌。紅線照舊:**寧缺勿造,每條可追溯**。

## 量測到的問題(這是結構病,不是翻譯債)

- 全庫 224 張方劑卡,**163 張的 `contraindications_en` 與 `cautions_en`
  逐位元組相同** —— 兩欄不是兩份獨立內容,是同一段英文被貼兩次,
  「禁用 vs 慎用」的方向詞是譯者當下隨手決定的。
- 後果:中英安全方向打架反覆出現(如甘遂卡曾把十八反寫成「降低藥效」,
  同組配伍在甘草卡是絕對禁用 —— A7 已修,但這類事會再生,因為結構允許)。
- 本庫已驗證的一條規律(D20):**一個欄位同時回答兩個問題,守門就失效。**

## 要你交付的(規則,不是內容)

1. **兩欄的語意定義**,一句話能判的那種。例如(僅示意,由你定案):
   - `contraindications` = 絕對方向:「此病人狀態下不得用此方」(孕婦禁/
     十八反/臟器衰竭級),違反即傷害;
   - `cautions` = 條件方向:「此狀態下需調整、監測或專業評估後用」。
2. **方向詞受控詞彙**(zh+en 對照):禁用/慎用/需監測/需炮製前提/
   運動員限制…each 對應哪一欄、英文標準用詞(contraindicated in /
   use caution in / requires monitoring…)。翻譯時方向詞**查表,不自由發揮**。
3. **單條敘述的形狀**:一條 = {對象狀態, 方向詞, 理由(可省), 來源層級標籤}。
   來源層級沿用你 A2 包已建立的三層:formula-specific / ingredient-derived /
   modern-component —— 兩包規則必須互通,63 卡回填會同時用到兩份。
4. **既有 163 張的遷移規則**:機器可執行的判定表 ——
   哪些關鍵詞/句式歸禁用、哪些歸慎用、判不動的留 `needs_review` 誠實旗標
   (不硬分)。我方照表跑批次,判不動清單回給你二審。
5. **邊界案例裁法**(至少涵蓋):十八反類配伍禁忌(已另有結構化欄,
   文字欄怎麼引用不重複)、孕婦(禁 vs 慎的分界)、劑量條件式禁忌
   (「大劑量久服」類)、運動員/法規類限制歸哪欄。

## 已知約束(設計時請對齊,不必重查)

- 形狀公約(`data/config/field_shape_convention.json`):安全類欄位一律陣列。
- 十八反/十九畏已有結構化關係與 CI 檢查(`herb_incompatibility_pairs.json`);
  文字欄只做 UI 呈現,不做 canonical 承載 —— 與你 A2 包 schema_F 的建議一致。
- 無來源的既有句在遷移時**不升級方向**(慎用不因改寫變禁用,反之亦然),
  方向變更一律需要來源。

## 交付格式

沿用 A2 包慣例:ZIP(`docs/research_packs/*.md` + `data/research_staging/*.json`
+ SHA256SUMS)。`NO PHI`、`NOT CANONICAL`。規則文件為主,不需要逐卡內容。
