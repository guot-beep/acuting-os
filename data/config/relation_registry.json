{
 "dataset": "Relation registry — the knowledge graph's edge list (DECISIONS D13)",
 "policy": [
  "Every graph edge is STORED on exactly one side (the referencing side) and DERIVED on the other. Storing both directions by hand means the two sides drift within a year and no validator can say which one is right.",
  "This file is the authority on which fields are edges. CG4's reverse index, the future graph UI, and validators all enumerate edges from here — adding a link field to any template without registering it here means the graph silently cannot see it.",
  "Knowledge→knowledge reverses are derived at build time (e.g. pattern_registry.used_by_conditions). Clinical→knowledge reverses are derived at RUNTIME only, never persisted into knowledge records (D9).",
  "Symmetric edges (differential_patterns, comparison membership) are stored once on one record and rendered on both — never mirrored by hand.",
  "Edges come in two kinds and they are NOT reverses of each other. DESCRIPTIVE edges answer \"which symptoms does this diagnosis have?\" and are stored on the diagnostic entity. INFERENTIAL edges answer \"what does this observation point to?\" and are stored on the observation. sym.differentiation[].points_to is inferential; edge.pattern_symptoms is descriptive. Deleting one as a duplicate of the other is a misreading of D13.",
  "Authored side is NOT decided by cardinality. It is decided by where the source material naturally puts the information and where an agent is already working when it fills the card. For symptoms all four tests agree: the diagnosis lists its symptoms."
 ],
 "created": "2026-08-06",
 "edges": [
  {
   "id": "edge.condition_patterns",
   "stored_on": "cond.*",
   "field": "related_patterns",
   "target": "pattern.*",
   "file": "data/pathology/condition_canon_shortlist.json",
   "reverse": {
    "derived_field": "used_by_conditions",
    "on": "data/pathology/pattern_registry.json",
    "derived_by": "scripts/build-pattern-registry.js"
   },
   "validated_by": "scripts/validate-condition-standard.js C6",
   "semantics": "possible_overlap — never one-to-one equivalence (D11)"
  },
  {
   "id": "edge.condition_tcm_diseases",
   "stored_on": "cond.*",
   "field": "related_eastern_diseases",
   "target": "tdis.*",
   "file": "data/pathology/condition_canon_shortlist.json",
   "reverse": {
    "derived_field": null,
    "note": "render-time only for now"
   },
   "validated_by": "scripts/validate-relations.js",
   "semantics": "possible_overlap — 月經過多(cond) and 月經過多(tdis) are two entities (D11)"
  },
  {
   "id": "edge.condition_formulas",
   "stored_on": "cond.*",
   "field": "herb_formulas",
   "target": "formula.* (aspirational — see stored_shape)",
   "file": "data/pathology/condition_canon_shortlist.json",
   "reverse": {
    "derived_field": null,
    "note": "formula cards derive related_conditions at render time"
   },
   "validated_by": "scripts/validate-relations.js",
   "stored_shape": "zh_name_strings",
   "shape_note": "Measured 2026-08-27 (D25 derived-governance sweep): all 2,902 entries across 172 records are Chinese formula NAMES (e.g. 溫經湯), not formula.* ids — the same pre-id class as acupoint_protocols below. The original declaration claimed ids aspirationally; validate-relations passes because it never resolved this field. Declared honestly; migrating names → formula.* ids is the condition line's work, not a registry edit.",
   "migration": "zh name strings -> formula.* ids. Until then this edge cannot be reverse-indexed."
  },
  {
   "id": "edge.condition_acupoint_protocols",
   "stored_on": "cond.*",
   "field": "acupoint_protocols",
   "target": "point id (D2)",
   "file": "data/pathology/condition_canon_shortlist.json",
   "reverse": {
    "derived_field": "related_conditions",
    "on": "data/acupoints/361.json",
    "derived_by": "scripts/link-point-conditions.js --apply (UNION writes — never removes; historical links survive cond-side redirects by design)",
    "note": "Corrected 2026-08-27 (D25 sweep): this note used to claim render-time derivation, but the reverse has been PERSISTED onto point records since §6.5 B. Freshness measured 2026-08-27: 179/185 points in full sync, 1 refreshed same day, 5 carry union residue from D23 redirects. Rerun the script after cond-side protocol changes."
   },
   "validated_by": "scripts/validate-relations.js",
   "stored_shape": "inline_objects",
   "shape_note": "Measured 2026-08-06 by validate-relation-registry R4: this field holds inline objects ({name_zh, code}), not ids — e.g. cond.endometriosis carries {\"name_zh\":\"期門\",\"code\":\"LV14\"}. It is the same pre-id shape as cond.tcm_patterns. Declared honestly rather than aspirationally; migrating it to point ids is the condition line's work, not a registry edit.",
   "migration": "inline objects -> point ids (D2). Until then this edge cannot be reverse-indexed."
  },
  {
   "id": "edge.condition_tcm_patterns_preid",
   "stored_on": "cond.*",
   "field": "tcm_patterns",
   "target": "pattern.* (aspirational — see stored_shape)",
   "file": "data/pathology/condition_canon_shortlist.json",
   "reverse": {
    "derived_field": "tcm_pattern_ids",
    "on": "data/acupoints/361.json (projection: cond.tcm_patterns[].acupoints_zh → the named points)",
    "derived_by": "scripts/link-point-conditions.js --apply (UNION writes)",
    "note": "Corrected 2026-08-27: a derived projection of this edge IS persisted — 44 points carry tcm_pattern_ids. 127 stored entries are retired pat.<中文> ids written by a pre-guard run; that is D23's B-bucket (ruled: fill line via pattern_alias_map, unmapped stay pending, never force-matched). The current script refuses to write legacy ids."
   },
   "validated_by": "scripts/validate-relation-registry.js R4/N3 (shape honesty only — no validator resolves the content)",
   "stored_shape": "inline_objects",
   "shape_note": "Registered 2026-08-27 (D25 derived-governance sweep): 720 inline differentiation objects ({pattern_zh, formula_zh, symptoms_zh}) across 150 records, zero carry an id. This is the pre-id form of edge.condition_patterns and is ACTIVELY RENDERED by app.js (7 call sites) — it is content, not a dead legacy field, so it is registered as an edge with an honest shape rather than parked in deprecated_but_temporarily_accepted. It had never been registered here at all, which is how the graph could not see 720 pattern links.",
   "migration": "pre-id objects -> related_patterns ids + structured content (fill line). NO new records may add tcm_patterns — new pattern links go in related_patterns."
  },
  {
   "id": "edge.condition_medications",
   "stored_on": "cond.*",
   "field": "medication_links",
   "target": "med.*",
   "file": "data/pathology/condition_canon_shortlist.json",
   "reverse": {
    "derived_field": null,
    "note": "medication layer not built yet — target namespace reserved"
   },
   "validated_by": "scripts/validate-relations.js"
  },
  {
   "id": "edge.pattern_formulas",
   "stored_on": "pattern.*",
   "field": "typical_formulas",
   "target": "formula.*",
   "file": "data/pathology/pattern_library.json",
   "reverse": {
    "derived_field": null,
    "note": "formula cards derive at render time"
   },
   "validated_by": "scripts/validate-pattern-standard.js P6 (N2 counts emptiness)"
  },
  {
   "id": "edge.pattern_points",
   "stored_on": "pattern.*",
   "field": "typical_points",
   "target": "point id (D2)",
   "file": "data/pathology/pattern_library.json",
   "reverse": {
    "derived_field": null,
    "note": "point cards derive at render time"
   },
   "validated_by": "scripts/validate-pattern-standard.js P6 (N2 counts emptiness)"
  },
  {
   "id": "edge.pattern_differentials",
   "stored_on": "pattern.*",
   "field": "differential_patterns[].pattern_id",
   "target": "pattern.*",
   "stored_shape": "inline_objects",
   "shape_note": "全庫 144 卡/263 項一致為鑑別物件(id key = pattern_id,含 distinguishing_zh/en、舌脈差異、source)— 2026-08-12 修正原宣告(誤稱裸 id);反向索引需以 pattern_id 萃取",
   "file": "data/pathology/pattern_library.json",
   "reverse": {
    "derived_field": null,
    "note": "SYMMETRIC: stored once on the record where it was authored, rendered on both patterns. Never hand-mirror onto the counterpart."
   },
   "validated_by": "scripts/validate-pattern-standard.js P6",
   "field_note": "2026-08-19: was \"differential_patterns\" — the field holds inline objects whose id lives in .pattern_id; the path form makes R4 resolve ids the way sym.differentiation_zh[].points_to already does."
  },
  {
   "id": "edge.comparison_members",
   "stored_on": "cmp.*",
   "field": "compares",
   "target": "pattern.* | formula.* (by record type)",
   "file": "data/knowledge/comparisons.json",
   "reverse": {
    "derived_field": "used_by_comparisons",
    "on": "data/pathology/pattern_registry.json (pattern members only)",
    "derived_by": "scripts/build-pattern-registry.js --refresh-counts",
    "note": "Count only — member cards' comparison-table chips remain render-time. Declared 2026-08-26: the field had been persisted since 2026-07-31 but never registered here, which is how its staleness went unnoticed (D25 supplements)."
   },
   "validated_by": "scripts/validate-relations.js validateComparisons"
  },
  {
   "id": "edge.clinical_case_links",
   "stored_on": "clinical layer (localStorage → future SQLite)",
   "field": "case_links.* / visit_* junctions",
   "target": "cond.* | tdis.* | pattern.* | formula.* | herb.* | point id | med.*",
   "file": "data/clinical_cases/schema.sql (+ acuting-clinical-cases-v1)",
   "reverse": {
    "derived_field": null,
    "note": "RUNTIME ONLY (D9): the 'used in 18 cases' panel queries the clinical store at render; aggregates are never written into knowledge records. Dated snapshots go to data/audits/clinical_usage_snapshot.json only. Distinct from D9's scope: pattern_registry.used_by_cases counts ONLY the repo's seed/sample case files (data/clinical_cases/*.json, template files excluded) via scripts/build-pattern-registry.js --refresh-counts — repo data, not the clinical store; the runtime store is never scanned or persisted (D25 supplement 2)."
   },
   "validated_by": "scripts/validate-clinical-case-standard.js F-series"
  },
  {
   "id": "edge.condition_symptoms",
   "stored_on": "cond.*",
   "field": "sign_symptom_ids",
   "target": "sym.*",
   "file": "data/pathology/condition_canon_shortlist.json",
   "edge_kind": "descriptive",
   "reverse": {
    "derived_field": "seen_in_conditions",
    "on": "sym.* card (display only)",
    "derived_by": "render-time join"
   },
   "validated_by": "scripts/validate-condition-standard.js (id resolution) + validate-symptom-standard.js Y8 (rejects hand-filled reverse)",
   "rationale": "Any migraine source lists unilateral headache / nausea / photophobia. No source is organised as \"headache appears in which diseases\". Storing on sym.* would mean building one migraine card requires editing three symptom cards.",
   "migration": "cond.related_tcm_symptoms (1/150 filled) is the pre-D13 form of this edge and is retired in favour of sign_symptom_ids."
  },
  {
   "id": "edge.pattern_symptoms",
   "stored_on": "pattern.*",
   "field": "key_signs_ids",
   "target": "sym.*",
   "file": "data/pathology/pattern_library.json",
   "edge_kind": "descriptive",
   "reverse": {
    "derived_field": "seen_in_patterns",
    "on": "sym.* card (display only)",
    "derived_by": "render-time join"
   },
   "validated_by": "scripts/validate-pattern-standard.js (id resolution) + validate-symptom-standard.js Y8",
   "rationale": "中診 textbooks are organised as \"肺氣虛: 咳喘無力、氣短聲低、自汗畏風\". The fill line reads by pattern, not by symptom.",
   "migration": "pattern_library.key_signs_zh holds 182 free-text strings. key_signs_ids is ADDITIVE — key_signs_zh stays as display fallback until sym.* coverage is sufficient. Never delete it (§0 只加深不刪除)."
  },
  {
   "id": "edge.tdis_symptoms",
   "stored_on": "tdis.*",
   "field": "key_manifestation_ids",
   "target": "sym.*",
   "file": "data/pathology/tdis_registry.json",
   "edge_kind": "descriptive",
   "reverse": {
    "derived_field": "seen_in_tdis",
    "on": "sym.* card (display only)",
    "derived_by": "render-time join"
   },
   "validated_by": "scripts/validate-tdis-standard.js (id resolution) + validate-symptom-standard.js Y8",
   "rationale": "中醫內科學 is likewise organised from disease name down to manifestations.",
   "migration": "tdis has no manifestation content yet — zero migration cost."
  },
  {
   "id": "edge.tdis_patterns",
   "stored_on": "tdis.*",
   "field": "related_patterns",
   "target": "pattern.*",
   "file": "data/pathology/tdis_registry.json",
   "edge_kind": "descriptive",
   "reverse": {
    "derived_field": null,
    "note": "pattern cards derive their 所屬病 chips at render time"
   },
   "validated_by": "scripts/validate-tdis-standard.js (id resolution; N1 counts emptiness)",
   "rationale": "辨證分型 — 中醫內科學 organises each disease into its pattern types; this is the tdis card's core section, authored content (空卡填補 batch 1-8), not a back-link.",
   "registered": "2026-08-26 derived-governance sweep: 609 edges across 159/160 records had been live since the fill batches but the edge was never registered here — the registry's own policy says an unregistered link field is invisible to the graph."
  },
  {
   "id": "edge.tdis_differentials",
   "stored_on": "tdis.*",
   "field": "differential_diseases",
   "target": "tdis.*",
   "file": "data/pathology/tdis_registry.json",
   "reverse": {
    "derived_field": null,
    "note": "SYMMETRIC like edge.pattern_differentials: stored once where authored, rendered on both. Never hand-mirror."
   },
   "validated_by": "scripts/validate-tdis-standard.js",
   "measured": "0/160 filled, 2026-08-26 — declared for the template, not yet in use"
  },
  {
   "id": "edge.tdis_formulas",
   "stored_on": "tdis.*",
   "field": "typical_formulas",
   "target": "formula.*",
   "file": "data/pathology/tdis_registry.json",
   "reverse": {
    "derived_field": null,
    "note": "formula cards derive at render time"
   },
   "validated_by": "scripts/validate-tdis-standard.js",
   "measured": "0/160 filled, 2026-08-26 — 總綱層 links live on pattern cards; declared for the template"
  },
  {
   "id": "edge.tdis_points",
   "stored_on": "tdis.*",
   "field": "typical_points",
   "target": "point id (D2)",
   "file": "data/pathology/tdis_registry.json",
   "reverse": {
    "derived_field": null,
    "note": "point cards derive at render time"
   },
   "validated_by": "scripts/validate-tdis-standard.js",
   "measured": "0/160 filled, 2026-08-26 — declared for the template"
  },
  {
   "id": "edge.formula_composition",
   "stored_on": "formula.*",
   "field": "composition[].herb_id",
   "target": "herb.*",
   "file": "data/herbs/formulas.json",
   "edge_kind": "descriptive",
   "stored_shape": "inline_objects",
   "shape_note": "Entries are objects carrying herb_id plus display fields (herb_zh/name_en/pinyin/dose) — id-bearing, unlike the pre-id shapes elsewhere. 1,637 herb_id edges measured 2026-08-27.",
   "reverse": {
    "derived_field": null,
    "note": "Render-time only: a formula's herb list renders from composition itself; a herb card wanting '出現在哪些方' derives it from composition at render. RULED D26 (Ting, 2026-08-27): edge.herb_related_formulas is a DIFFERENT edge (curated study links), NOT this edge's reverse — the ~48% overlap and the 846 composition edges without a herb-side entry are BY DESIGN (curation is selective), not drift. Neither field may be regenerated from the other."
   },
   "validated_by": "scripts/validate-formula-standard.js (composition checks)",
   "registered": "2026-08-27 D25 derived-governance sweep — no formula/herb edge had ever been registered here."
  },
  {
   "id": "edge.herb_related_formulas",
   "stored_on": "herb.*",
   "field": "related_formulas",
   "target": "formula.*",
   "file": "data/herbs/herb_canon_shortlist.json",
   "reverse": {
    "derived_field": null,
    "note": "formula cards derive herb chips at render time"
   },
   "validated_by": "scripts/validate-herb-canon.js (id resolution — 1,703 edges, 0 dangling measured 2026-08-27)",
   "edge_kind": "descriptive",
   "semantics": "RULED D26 (Ting, 2026-08-27:「herb側是策展連結不是組成反向」): curated study links — the formulas worth knowing this herb through (代表方/研讀連結), authored on the herb card. NOT the reverse of formula.composition and NOT exhaustive membership: a linked formula need not contain the herb (加減/代表用法 are legitimate), and a formula containing the herb need not be linked (curation is selective). The ~48% overlap with composition measured 2026-08-27 is therefore expected, not drift — no reconciliation work exists. Never bulk-regenerate this field from composition; membership questions are answered by composition, study-link questions by this field."
  },
  {
   "id": "edge.formula_related_formulas",
   "stored_on": "formula.*",
   "field": "related_formulas",
   "target": "formula.*",
   "file": "data/herbs/formulas.json",
   "reverse": {
    "derived_field": null,
    "note": "SYMMETRIC study/differentiation links: stored where authored, rendered on both. 417 edges, 0 dangling measured 2026-08-27."
   },
   "validated_by": "scripts/validate-relations.js"
  },
  {
   "id": "edge.formula_patterns",
   "stored_on": "formula.*",
   "field": "tcm_pattern_ids",
   "target": "pattern.*",
   "file": "data/herbs/formulas.json",
   "edge_kind": "descriptive",
   "reverse": {
    "derived_field": null,
    "note": "pattern cards derive at render time"
   },
   "validated_by": "(none resolves this field — 80 edges, 0 dangling measured 2026-08-27)",
   "registered": "2026-08-27 D25 sweep. Distinct from data/herbs/formula_pattern_links.json (edge.formula_pattern_links_file)."
  },
  {
   "id": "edge.formula_pattern_links_file",
   "stored_on": "formula.* (junction file)",
   "field": "pattern_ids",
   "field_note": "records also carry western_condition_ids (cond.*) — declared here once for the file; the validator resolves pattern_ids.",
   "target": "pattern.* | cond.*",
   "file": "data/herbs/formula_pattern_links.json",
   "reverse": {
    "derived_field": null,
    "note": "10-record fertility-track junction file with its own clinical_authority_notice; read by validate-relations."
   },
   "validated_by": "scripts/validate-relations.js"
  },
  {
   "id": "edge.symptom_pattern_inference",
   "stored_on": "sym.*",
   "field": "differentiation_zh[].points_to",
   "target": "pattern.*",
   "file": "data/symptoms/symptoms.json",
   "file_note": "Created 2026-08-06 by Pilot 0 (3 records). The path carried the prose \"(not yet created)\" inside it until then, which meant the registry's own validator could not resolve the one relation a symptom card authors — machine-readable fields must not carry commentary.",
   "edge_kind": "inferential",
   "reverse": {
    "derived_field": null,
    "note": "No reverse. This is NOT the mirror of edge.pattern_symptoms — an inferential edge and a descriptive edge are different claims (see policy)."
   },
   "validated_by": "scripts/validate-symptom-standard.js Y6",
   "rationale": "\"脹痛 points to 氣滯\" is clinical reasoning authored on the symptom card. \"氣滯證 presents with 脹痛\" is a textbook list authored on the pattern card. Different claims, different provenance, both needed.",
   "path_form": "nested: differentiation_zh[].points_to — verified parseable by scripts/validate-relation-registry.js on a temporary fixture 2026-08-06 (2 variants yielded 3 pattern ids, not objects). A consumer using record[field] naively would get the object array instead."
  }
 ],
 "retired_hand_filled_fields": [
  {
   "field": "pattern_library.related_conditions",
   "reason": "The reverse of edge.condition_patterns. 0/50 records had it filled when D13 locked (2026-08-06), so it was retired from hand-filling at zero cost — hand-maintaining both sides is exactly the drift D13 forbids. It is now derived-only (pattern_registry.used_by_conditions carries it)."
  },
  {
   "field": "sym.seen_in_conditions / seen_in_patterns / seen_in_tdis",
   "reason": "All three are reverses of descriptive edges stored on the diagnostic entity. Derived at render time; hand-filling any of them is validate-symptom-standard Y8."
  },
  {
   "field": "tdis_registry.related_conditions",
   "reason": "The reverse of edge.condition_tcm_diseases (this registry made cond.* the stored side, 2026-08-06). 0/160 records had it filled when retired 2026-08-26 (derived-governance sweep), so it was retired from hand-filling at zero cost — the identical situation, and identical fix, as pattern_library.related_conditions above. validate-tdis-standard T8 now flags it."
  }
 ],
 "deprecated_but_temporarily_accepted": [
  {
   "field": "cond.related_tcm_symptoms",
   "superseded_by": "cond.sign_symptom_ids (edge.condition_symptoms)",
   "state": "deprecated_but_temporarily_accepted",
   "rules": [
    "NO new content may use it. New symptom links go in sign_symptom_ids.",
    "The 1 existing record (cond.functional_dyspepsia) may keep its value until the fill line migrates it — deleting it now would lose the inline blob before its ids exist (§0 只加深不刪除).",
    "It stays in validate-condition-standard APPROVED so that record is not a defect. It is removed from APPROVED only after migration.",
    "A validator warning on the old value is acceptable; a blocking error is not, until migration is possible."
   ],
   "measured": "1/150 records filled, 2026-08-06",
   "differs_from_retired": "A RETIRED field is one nobody may write and nothing holds. This one still holds data, so it needs a third state — calling it retired while a validator still approves it is the contradiction this entry exists to prevent."
  }
 ]
}
