# 兩週衝刺 2026-08-26 → 09-09($200 額度最後兩週)

Ting 2026-08-26 裁定:「還有兩星期就要從 200 回去 20。優化 OS 跟架構完善,
製作卡片先暫停,還要搬去 SQLite。」本檔取代 `PLAN_TO_2026-09-05.md` 的排程部分
(該檔的掃描與分析仍有效);現行優先序仍以 `OPTIMIZATION_PLAN_2026-08.md` 頂表為準。

**排程邏輯**:9/01 UI 與臨床 schema 凍結(D12)、**9/02 進診所**(Ting 2026-08-26
更正:實際開診日是 9/2 不是 9/5,見 DECISIONS.md 更正註記)——
所以會動畫面的事必須在 9/01 前做完;凍結後那週全力做不碰畫面的底層。
$200→$20 意味著:這兩週優先做**吃大量高階 AI 工時**的工程;
人工節奏的事(RV1、內容裁定)不吃 token,留到降級後。

Owners:**C** = Claude · **T** = Ting。每天最多兩個主目標(build-heavy 紀律)。
小批 commit + push —— commit 不等於安全,本專案被洗掉過兩次。

---

## 第一週 8/26–9/01:凍結前,清掉所有還要動畫面的事

| # | 事 | Owner | 完成的定義 |
|---|---|---|---|
| W1-1 | **Pattern V2 收尾落地**:`codex/pattern-v2` 併回 main | C | main 含本分支全部 commit;merge 後照 [[merges-clobber-knowledge-js]] diff knowledge.js 各分頁 section |
| W1-2 | **OS 優化(只挑會動 UI 的部分)**:照 Core Loop → Visit Brief → Timeline 順序 | C | 每項各自的驗證器 PASS + 開畫面眼睛看過 |
| W1-3 | **Ting 裁定日(約 30 分鐘,不花 token 但擋住 AI)** | T | SQLite 4 個 unresolved 欄位(`MIGRATION_LOCALSTORAGE_TO_SQLITE.md` §3)+ `TING_DECISION_QUEUE.md` A 類逐項有裁定 |
| W1-4 | **9/01 執行凍結**:之後 UI 想法進 backlog 不進 code | C | 凍結後任何 `js/**`、`index.html`、`styles.css` 的變更只准修 bug |

## 第二週 9/02–9/09:凍結後,全力做不碰畫面的底層

| # | 事 | Owner | 完成的定義 |
|---|---|---|---|
| W2-1 | **SQLite「建好」**:遷移腳本 + adapter + 測試,照 `RUNTIME_ADAPTER_SPEC.md` 與 runtime contract 十原則;在演練資料上遷移一次並逐欄位核對 | C | 67 欄位對應全部有實作;演練資料 round-trip 無損;localStorage 保持可回退 |
| W2-2 | **七支紅燈驗證器接 CI**:照 `UNWIRED_VALIDATORS_2026-08-24.md` 五步(方劑空殼殘根 → 樣板殘字 → encoding 修規則 → herb-canon ratchet → 其餘兩支同 ratchet) | C(內容裁定歸 T) | 七支全部在 `validate.yml` 裡;ratchet baseline 記錄當前數字;`check-validation-ratchet.js` PASS |
| W2-3 | **9/02 開診,首週摩擦當場修** | T 用 + C 修 | 診所發現的問題優先於一切排程;每天留緩衝 |

**SQLite「建好」與「切換」分開(Ting 2026-08-26 核可)**:這兩週把 schema、
adapter、遷移腳本全部寫完並在演練資料上驗證通過;正式切換等開診第一週跑穩再按。
切換本身不吃 token,穩定性風險全在切換那一刻 —— AI 工時花在崖前,風險留在崖後。

## 明確暫停(9/09 前不做)

- 卡片量產(herb / formula / condition / pattern 全線)—— 留給 Codex/Antigravity 或降級後
- 西藥卡**內容**(架構量尺除外)
- 症狀 129 筆、方劑鑑別 30 張空表
- 9/01 後任何 UI 改動(修 bug 除外)

## 「這兩週做完」的定義

- [ ] `codex/pattern-v2` 已在 main
- [ ] Core Loop / Visit Brief / Timeline 的 UI 部分在 9/01 前落地
- [ ] SQLite 遷移在演練資料上跑過一次、round-trip 無損、可回退
- [ ] 61 支驗證器全部在 CI(直接擋或 ratchet 擋),0 支寫好沒接線
- [ ] 9/02 開診用的是穩定的 localStorage 線,SQLite 切換鍵留在手上
