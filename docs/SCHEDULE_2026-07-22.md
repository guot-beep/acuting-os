# Consolidated schedule — everything decided 2026-07-22

Ting: 「你別忘了今天所有的建議 跟改動 先設計好寫入排程」.

One session produced a policy change, two shipped features, four design tracks
and a quality reckoning. This is the single place that says what was decided,
what is done, what is next, and who owns it. **Read this before EXECUTION_PLAN.md
— it supersedes that file's ordering for everything listed here.**

Owners: **C** = Claude · **X** = Codex · **T** = Ting decision required.

---

## 0. The governing change — read this first

**The content policy was retired and replaced** (`AGENTS.md`, top of file).

Old default: leave the field empty with a 待補 placeholder, wait for a gate.
That default produced 202 herbs sharing 26 template sentences, which passed all
eight validators and got reported to Ting as complete.

New default: **fill it from a professional source, cite per field, render it as
draft, review it in-app.** An empty dosage field is not safer than a sourced
one — it just moves the lookup somewhere untracked. This app is private and
internal.

Still handled carefully — meaning *fill, cite and flag*, never blank: needling
depth and angle, herb toxicity and maximum dose, pregnancy cautions,
herb–drug interactions.

**Applies to Codex identically.** Stop producing staging previews that wait for
a gate.

---

## 1. Done today

| | Item | Commit |
| --- | --- | --- |
| C | Reviewed and accepted Codex `5af7892` (herb visual links), `16b7f11` (WHO staging), `27864b5` (high-risk anatomy), `33882b5` (protocol anatomy) | `4c8879a`, `b0b35bf`, `d9213ef` |
| C | Used WHO staging to adjudicate the two real CloudTCM §A conflicts — **BL4 our 3寸 is wrong, WHO and neighbours say 1.5**; SI16 our cun string duplicates LI18's, WHO uses landmarks only. Recommendations only, 361.json untouched | `a57bfee` |
| C | **CS6** — case and SOAP dialogs segmented; link fields moved into A-Assessment | `28e1440` |
| C | **RV1** — in-app review controls, verdicts to localStorage, `scripts/apply-review-verdicts.js` (dry-run default) | `7f8ff7a` |
| C | **`validate-content-quality.js`** — the substance check the repo never had | pushed |
| C | NCBAHM BIO 2026 blueprint as versioned data + `report-exam-coverage.js` | pushed |
| C | 藥對 records + 七情配伍 vocabulary | pushed |
| C | Reference 麻黃 record at target standard (1518 → 10646 chars) | pushed |
| C | AM0 movement vocabulary (46 bilingual terms) | pushed |
| C | Design docs: AM track, SMA track, FACE track, card spec, **CARD_REDESIGN_V2** | pushed |

### The numbers that matter

Content quality, measured not assumed:

| layer | substantive |
| --- | --- |
| acupoint functions / indications | **93–98%** — genuinely good |
| acupoint contraindications | 70% (107 share a template) |
| acupoint muscles / nerves / clinical_pearls | 0% / 0% / 2% |
| **herbs — every field** | **0%** |
| formulas composition | 20% (23/115) |
| conditions | 17% (25/150) |
| **overall** | **36%** |

NCBAHM BIO blueprint coverage: 118 official conditions, 53 matched by name
(45%), **5 with actual content (4%)**. Hematological and Genetics at zero.

---

## 2. Next — unblocked, no decision needed

Ordered so each step is visible before the next begins.

| # | Item | Owner | Notes |
| --- | --- | --- | --- |
| N1 | **Meridian ring component** | C | Pure derived render from `channels_entered`. No new data. Ting approved. Works on herb *and* acupoint cards. |
| N2 | Vitals pill row + toned pinyin + channel abbreviations | C | `glance` block already in the reference record |
| N3 | Formula 君臣佐使 roster — per-herb dose, role, one-line role reason | C | The single biggest teaching win in the redesign |
| N4 | CloudTCM herb URL map to full 202 coverage | X | 24 seeded, 23 match canon. Unlocks correct links *and* the per-herb Chinese source |
| N5 | Fix `js/knowledge.js` herb links to use direct URLs | X | Codex's file. Root cause: CloudTCM pages are `/herb/<numeric id>`, no name in URL |
| N6 | Herb content fill, first batch of 20, to the reference-record standard | X then C reviews | Bilingual, dosage, 炮製, 現代藥理, nested indications, per-field sources |
| N7 | ACPL exam blueprint → data + coverage report | C | Same treatment as BIO; defines official point/anatomy scope |
| N8 | AM1 muscle canon skeleton | C | Seed from the 11 already-sourced muscles in Codex's staging |

## 3. Dependent work

| # | Item | Blocked on |
| --- | --- | --- |
| D1 | Formula content fill, 92 empty skeletons | N6 proving the pipeline |
| D2 | 中西醫對照 + 現代應用 sections on cards | condition canon fill |
| D3 | Condition canon fill — prioritise the 65 blueprint gaps, Hematological and Genetics first | N7 |
| D4 | Herb profile radar | axis data assignment; do 解表藥 10 herbs first to calibrate |
| D5 | AM2–AM5 anatomy fill, point↔muscle junctions, muscle cards | AM1 |
| D6 | 經筋 sinew channel layer | AM muscle canon |
| D7 | Facial muscle canon (~18) | AM1 pattern established |
| D8 | Exam/assessment layer (special tests, imaging) | injury layer |

## 4. Waiting on Ting

Nothing below blocks §2. Collected here so it stops being scattered.

| # | Decision |
| --- | --- |
| T1 | BL4 and SI16 corrections — apply to 361.json? (WHO evidence recorded in the worksheet) |
| T2 | The 9 CloudTCM §B needling-depth conflicts — WHO carries locations, not depths, so this needs your textbook call |
| T3 | Chinese-language source for anatomy and for 運動醫學/美容針灸 — pick one, or I bring verified candidates |
| T4 | Is SMAC certification an actual goal? Changes whether the site mirrors its module structure |
| T5 | FACE — post-graduation goal? Affects whether FA is built now or parked as structure |
| T6 | 祛斑 — one aesthetic goal, or split by underlying diagnosis? (I recommend splitting) |
| T7 | Bone layer — landmarks only, or full attachment modelling? |
| T8 | Do you own any Callison material? Would let motor points be sourced properly |

**Strategic note for T4/T5:** ABORM (FABORM) is the certification you are
closest to — your only filled content batch is the 25 gyn conditions, and the
SOAP form already carries cycle day/phase and Western medication fields. SMAC
and FACE share faculty (Matt Callison) and one anatomy engine, so building AM
once serves both.

## 5. Standing rules adopted today

1. **Fill and cite. Never 待補.** (§0)
2. **Bilingual is structural, not aspirational.** A `_zh` field holding English
   is a defect. Knowledge links need at least one zh and one en source.
3. **No boilerplate ever.** If N records share a sentence it is a skeleton, and
   it is worse than empty because empty is honest.
4. **Final verification compares against an external professional site**, not
   only internal checks. A layer is not done while a benchmark source has whole
   sections we lack.
5. **Where two sources disagree, record both with attribution.** Never silently
   pick one, never present one source's number as consensus.
6. **Quality is measured, not asserted.** `validate-content-quality.js` before
   any "done".
7. **Links only, no copied images.** Direct URLs, never site-scoped search.

## 6. My own failures today, recorded so they are not repeated

- I accepted `5af7892` after noting in the same review that its scoped-search
  links were weak. I verified that links *rendered*, never that they *resolved*.
  Ting found them broken.
- I reported herbs as "202/202 complete" by counting non-empty fields. They
  were 26 templates. Coverage claims now require the quality validator.
- My first coverage script read `formulas.json`'s `starter_categories[8]`
  instead of `records[115]` — measuring the wrong array, the same class of
  error. Caught and fixed.
